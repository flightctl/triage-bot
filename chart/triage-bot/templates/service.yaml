apiVersion: v1
kind: Service
metadata:
  name: {{ include "triage-bot.fullname" . }}
  labels:
    {{- include "triage-bot.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "triage-bot.selectorLabels" . | nindent 4 }}
